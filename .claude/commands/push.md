---
description: Land changes on main via squash-merged PR, no release
argument-hint: "Brief commit message describing the changes"
---

Invoke the `push` skill with the Skill tool and follow it exactly.

Arguments: $ARGUMENTS