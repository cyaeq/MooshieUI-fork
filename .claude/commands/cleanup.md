---
description: Branch hygiene + bot PR comment triage
argument-hint: "Optional scope hint, e.g. 'release branches only' or 'all open PRs'"
---

Invoke the `cleanup` skill with the Skill tool and follow it exactly.

Arguments: $ARGUMENTS