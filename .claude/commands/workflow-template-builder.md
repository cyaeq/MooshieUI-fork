---
description: ComfyUI workflow JSON templates in src-tauri/src/templates/
argument-hint: "Describe the workflow change: mode/template name, nodes, params"
---

Invoke the `workflow-template-builder` skill with the Skill tool and follow it exactly.

Arguments: $ARGUMENTS