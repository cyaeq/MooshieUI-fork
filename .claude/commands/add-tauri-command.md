---
description: New Tauri command: Rust handler + lib.rs registration + TS wrapper
argument-hint: "Describe the command: name, parameters, return type, and what it does"
---

Invoke the `add-tauri-command` skill with the Skill tool and follow it exactly.

Arguments: $ARGUMENTS