---
description: New custom ComfyUI Python node + Rust registration + workflow hookup
argument-hint: "Describe the node: name, inputs, what it does, where it hooks in"
---

Invoke the `add-comfyui-node` skill with the Skill tool and follow it exactly.

Arguments: $ARGUMENTS